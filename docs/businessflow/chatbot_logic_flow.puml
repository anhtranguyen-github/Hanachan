@startuml
title Sơ đồ hoạt động - Cách Trợ lý Hanachan hỗ trợ bạn
skinparam shadowing false
skinparam ArrowColor #FF69B4
skinparam ActivityBackgroundColor #FFF0F5
skinparam ActivityBorderColor #FF69B4
skinparam DefaultFontName "Segoe UI"

start
:Người dùng gửi câu hỏi/yêu cầu;

:Xem lại "Bản ghi nhớ" hiện tại\n(Các mục tiêu và quyết định đã chốt);

:Kiểm tra vài câu chuyện gần nhất\nđể nắm bắt ngữ cảnh hội thoại;

if (Yêu cầu cần thông tin tiếng Nhật chuẩn?) then (Có)
  :Tự động tra cứu trong "Kho giáo trình"\n(Hán tự, Từ vựng, Ngữ pháp);
  :Lấy dữ liệu chính xác kèm mẹo học tập;
else (Không)
  :Sử dụng kiến thức chung để giải đáp;
endif

:Chuẩn bị câu trả lời thân thiện (🌸);

if (Phát hiện thay đổi mục tiêu hoặc quyết định mới?) then (Có)
  :Ngầm cập nhật lại "Bản ghi nhớ"\n(Tránh việc phải hỏi lại người dùng sau này);
  :Cập nhật tiêu đề phiên trò chuyện phù hợp;
endif

:Gửi phản hồi kèm các "Nút học tập" (CTA);

:Người dùng nhận câu trả lời và tiếp tục học;
stop
@enduml

{
  "name": "hanachan",
  "version": "0.1.0",
  "private": true,
  "description": "Hanachan - Japanese language learning chatbot",
  "scripts": {
    "dev": "./scripts/run.sh",
    "test": "pnpm run test:all",
    "test:all": "pnpm run test:backend && pnpm run test:frontend",
    "test:backend": "cd src/fastapi && uv run python -m pytest",
    "test:backend:watch": "cd src/fastapi && uv run ptw",
    "test:backend:coverage": "cd src/fastapi && uv run python -m pytest --cov",
    "test:frontend": "cd src/nextjs && pnpm run test:unit",
    "test:frontend:watch": "cd src/nextjs && pnpm run test:watch",
    "test:frontend:coverage": "cd src/nextjs && pnpm run test:coverage",
    "test:unit": "pnpm run test:backend && pnpm run test:frontend:unit",
    "test:integration": "pnpm run test:frontend:integration",
    "lint": "pnpm run lint:backend && pnpm run lint:frontend",
    "lint:backend": "cd src/fastapi && uv run ruff check .",
    "lint:frontend": "cd src/nextjs && pnpm run lint",
    "preinstall": "pnpm dlx only-allow pnpm"
  },
  "devDependencies": {
    "@kilocode/cli": "^7.0.33"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@10.24.0"
}